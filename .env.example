# Application Ports
PORT=8080
FRONTEND_PORT=80
GIN_MODE=release

# Database Configuration
# For Docker: Use host.docker.internal to connect to PostgreSQL on host machine
# For local development: Use localhost or your PostgreSQL container name
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=voucher_db
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRATION=24h

# CORS Configuration
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:5173